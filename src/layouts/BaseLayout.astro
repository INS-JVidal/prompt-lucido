---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { t, type Lang } from '../i18n/utils';

interface Props {
	title: string;
	description?: string;
	lang?: string;
	translationUrl?: string;
}

const {
	title,
	lang = 'es',
	translationUrl,
} = Astro.props;

const description = Astro.props.description ?? t('site.description', lang as Lang);
---

<!doctype html>
<html lang={lang}>
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />
	<meta name="description" content={description} />
	<link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
	<meta name="generator" content={Astro.generator} />
	<title>{title}</title>
	<script src="https://analytics.ahrefs.com/analytics.js" data-key="Gho9WbhL3Clu1lxsVAV7aA" async></script>
</head>
<body>
	<Header lang={lang} translationUrl={translationUrl} />
	<main>
		<slot />
	</main>
	<Footer />
</body>
</html>
